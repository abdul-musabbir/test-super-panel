import StoreWebhook from './StoreWebhook'
import WebhookAction from './WebhookAction'
import WebhookReceiver from './WebhookReceiver'
const Actions = {
    StoreWebhook: Object.assign(StoreWebhook, StoreWebhook),
WebhookAction: Object.assign(WebhookAction, WebhookAction),
WebhookReceiver: Object.assign(WebhookReceiver, WebhookReceiver),
}

export default Actions